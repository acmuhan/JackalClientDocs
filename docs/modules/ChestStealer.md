ChestStealer
箱子小偷
分类：未分类
描述：暂无描述

需求
- 安全级别：常规模块
- 权限需求：无
- 驱动依赖：否
- 联网需求：否
- 开发状态：稳定/常规
- 版本属性：普通可用

介绍
ChestStealer 是 JackalClient 的 未分类 分类模块。启用后会围绕 箱子小偷 这一能力工作，核心行为是：暂无描述

配置项
- Semi-Auto Y Offset Slots (Dispenser)：类型=数值，默认=4.10f
- Drop Items Key：类型=按键/复合，默认={{"Keybind", {VK_LCONTROL, 'Q'}}}
- Notify Mode：类型=枚举，默认="Notify"
- Auto Close Key：类型=按键/复合，默认={{"Keybind", {'E'}}}
- Manual X Offset (Dispenser)：类型=数值，默认=-78
- Interrupt Key 1：类型=按键/复合，默认={{"Keybind", {'E'}}}
- Manual Y Offset (Large Chest)：类型=数值，默认=-278
- Manual X Offset (Hopper)：类型=数值，默认=-132
- Semi-Auto Y Offset Slots (Large Chest)：类型=数值，默认=4.60f
- Minecraft Window Check：类型=布尔，默认=true
- Item Operation：类型=枚举，默认="Steal"
- Auto Close：类型=布尔，默认=true
- Key Internal Gap 2 (ms)：类型=数值，默认=40L
- Operate Delay Max (ms)：类型=数值，默认=250
- HUD：类型=枚举，默认="Off"
- Manual X Offset (Small Chest)：类型=数值，默认=-240
- Start Delay Min (ms)：类型=数值，默认=100
- Slot Size：类型=数值，默认=52
- Semi-Auto Y Offset Slots (Hopper)：类型=数值，默认=2.00f
- HUD Font Size：类型=数值，默认=30
- Start Delay Max (ms)：类型=数值，默认=200
- Slot Internal Random Offset：类型=布尔，默认=true
- Semi-Auto Y Offset Slots (Small Chest)：类型=数值，默认=3.12f
- Manual Y Offset (Small Chest)：类型=数值，默认=-198
- Slot Order：类型=枚举，默认="Random"
- Trigger Mode：类型=枚举，默认="Hotkey"
- Mouse Easing Duration (ms)：类型=数值，默认=50L
- Click Internal Gap (ms)：类型=数值，默认=40L
- Slot No Empty Single Channel Threshold：类型=数值，默认=1000
- Manual X Offset (Large Chest)：类型=数值，默认=-240
- Async：类型=布尔，默认=true
- Manual Y Offset (Hopper)：类型=数值，默认=-141
- Operate Delay Min (ms)：类型=数值，默认=150
- Interrupt Key 2：类型=按键/复合，默认={{"Keybind", {VK_ESCAPE}}}
- Manual Y Offset (Dispenser)：类型=数值，默认=-198
- Trigger Hotkey：类型=按键/复合，默认={{"Keybind", {'B'}}}
- Coordinate Mode：类型=枚举，默认="Semi-Auto"
- End Delay Max (ms)：类型=数值，默认=200
- Container Type：类型=枚举，默认="Small Chest"
- Key Internal Gap 1 (ms)：类型=数值，默认=40L
- Slot Selector：类型=枚举，默认="No Empty Detect"
- HUD Target Slot Color：类型=枚举，默认="Rainbow"
- Trigger Hotkey Interrupt：类型=枚举，默认="Close Container"
- Not Trigger While Moving：类型=布尔，默认=true
- End Delay Min (ms)：类型=数值，默认=100
- Mouse Easing Movement：类型=枚举，默认="EaseInOutSine"

历史更新
- 28. 添加模块：ChestStealer，这里不展开描述。

备注
该模块可能受系统版本、权限级别、目标进程状态或安全软件策略影响；若功能未生效，优先检查管理员权限、驱动依赖、联网状态与系统兼容性。

相关命令
无

相关模块
无

相关资料
无
