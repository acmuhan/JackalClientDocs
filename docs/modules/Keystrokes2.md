Keystrokes2
按键显示II
分类：Control
描述：暂无描述

需求
- 安全级别：常规模块
- 权限需求：无
- 驱动依赖：否
- 联网需求：否
- 开发状态：稳定/常规
- 版本属性：普通可用

介绍
Keystrokes2（按键显示II）用于暂无描述。
适合键鼠行为控制、输入增强和自动化操作场景。
初次使用可优先调整：Multi Row Mode、Toggle KeyReaction Module、Rain Effect Coordinate Mode。

配置项
- Box Width（方框宽度）
 类型：数值；默认：55
 说明：这是数值型配置。默认值 55 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Box Height（方框高度）
 类型：数值；默认：70
 说明：这是数值型配置。默认值 70 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Box Gap（方框间隙）
 类型：数值；默认：15
 说明：这是数值型配置。默认值 15 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Box Opacity (0~1)（方框不透明度 (0~1)）
 类型：数值；默认：1.0f
 说明：用于控制透明度。默认值 1.0f 兼顾可见性和遮挡；如果你觉得挡视线可小幅下调，若看不清可小幅上调。
- Text Color（文本颜色）
 类型：枚举；默认："Rainbow"
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
 可选：Flow（流动）；其余颜色见 [NAMED_COLOR_BASE_LIST](./NAMED_COLOR_BASE_LIST.md)
- Outline Color（轮廓颜色）
 类型：枚举；默认："Rainbow"
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
 可选：Flow（流动）；Transparent（透明）；其余颜色见 [NAMED_COLOR_BASE_LIST](./NAMED_COLOR_BASE_LIST.md)
- Background Color（背景颜色）
 类型：枚举；默认："Black"
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
 可选：Transparent（透明）；其余颜色见 [NAMED_COLOR_BASE_LIST](./NAMED_COLOR_BASE_LIST.md)
- Font Size Rate（字号比例）
 类型：数值；默认：1.5
 说明：用于控制文本可读性。默认字号 1.5 适合多数分辨率；高分屏可适当加大，低分辨率建议减少以免拥挤。
- Bigger Font Size Of Single Char Key（让单字符键字号更大）
 类型：布尔；默认：false
 说明：用于控制文本可读性。默认字号 false 适合多数分辨率；高分屏可适当加大，低分辨率建议减少以免拥挤。
- X Rate（横坐标比例）
 类型：数值；默认：0.5f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 0.5f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Y Rate（纵坐标比例）
 类型：数值；默认：0.87f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 0.87f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Alignment（对齐方式）
 类型：枚举；默认："Center"
 说明：这是选项型配置。默认值 Center 一般更稳妥；建议按使用场景逐个试用，而不是一次性切换多项。
 可选：Center（居中）；Left（左侧）；Right（右侧）
- Hide When Menu On（菜单打开时不显示）
 类型：布尔；默认：false
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Easing Speed (0~1)（缓动速率 (0~1)）
 类型：数值；默认：0.1f
 说明：这是数值型配置。默认值 0.1f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Show Exit Animation（展示退场动画）
 类型：布尔；默认：true
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Exit Animation Duration (ms)（退场动画时长（毫秒））
 类型：数值；默认：220L
 说明：用于控制检测/刷新/动画节奏。默认值 220L 以稳定为主；调小会更灵敏但可能增加资源占用，调大则更省资源但响应更慢。
- Key Display Max Count（键显示最大数量）
 类型：数值；默认：10U
 说明：这是数值型配置。默认值 10U 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Key Display Order（键显示顺序）
 类型：枚举；默认："Layout"
 说明：这是选项型配置。默认值 Layout 一般更稳妥；建议按使用场景逐个试用，而不是一次性切换多项。
 可选：ASCII（未收录）；List Forward（未收录）；List Backward（未收录）；Layout（布局）
- Multi Row Mode（多排模式）
 类型：布尔；默认：false
 说明：这是该模块的核心行为开关。默认值 false 通常更稳，建议先验证默认策略再尝试其他模式。
- Multi Row Row Gap（排间距）
 类型：数值；默认：45
 说明：这是数值型配置。默认值 45 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Multi Row Offset (-1~1)（排偏移(-1~1)）
 类型：数值；默认：-0.3f
 说明：这是数值型配置。默认值 -0.3f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Multi Row Vertical Sort (PRO)（排纵向排序（专业版））
 类型：枚举；默认："Top"
 说明：这是选项型配置。默认值 Top 一般更稳妥；建议按使用场景逐个试用，而不是一次性切换多项。
 可选：None（无）；Top（顶端）；Bottom（底部）
- Multi Row Fog Enabled（多排是否启用迷雾）
 类型：布尔；默认：false
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Multi Row Fog Intensity (0~1)（多排迷雾强度）
 类型：数值；默认：0.6f
 说明：这是数值型配置。默认值 0.6f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Bigger Pressed（更大的按下）
 类型：布尔；默认：true
 说明：这是开关型配置。默认值 true 代表作者推荐的初始行为；若要改动，建议一次只改一个开关便于观察影响。
- Give Way to Mouse（给鼠标让道）
 类型：布尔；默认：true
 说明：这是开关型配置。默认值 true 代表作者推荐的初始行为；若要改动，建议一次只改一个开关便于观察影响。
- Give Way to Mouse Distance（给鼠标让道的距离）
 类型：数值；默认：200.0
 说明：这是数值型配置。默认值 200.0 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Show Number Below（显示底部数字）
 类型：布尔；默认：true
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Number Display Recent（数字显示最近次数）
 类型：布尔；默认：true
 说明：这是开关型配置。默认值 true 代表作者推荐的初始行为；若要改动，建议一次只改一个开关便于观察影响。
- Number Color Show Recent（数字颜色显示最近次数）
 类型：布尔；默认：true
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
- Hide Mouse Buttons（隐藏鼠标按钮）
 类型：布尔；默认：false
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Hide F13-F24（隐藏F13到F24）
 类型：布尔；默认：true
 说明：这是行为开关项。建议先按默认值使用，确认行为符合预期后再逐项启停，避免多个开关同时改动造成排查困难。
- Toggle KeyReaction Module（切换按键反应模块）
 类型：布尔；默认：true
 说明：这是该模块的核心行为开关。默认值 true 通常更稳，建议先验证默认策略再尝试其他模式。
- Round Corners（圆角）
 类型：布尔；默认：false
 说明：这是开关型配置。默认值 false 代表作者推荐的初始行为；若要改动，建议一次只改一个开关便于观察影响。
- Roundness（圆滑程度）
 类型：数值；默认：0.2f
 说明：用于细调显示样式和间距。默认值 0.2f 通常是平衡视觉效果与紧凑度的设置，建议小步调整并实时观察。
- Shadow（阴影）
 类型：布尔；默认：true
 说明：这是开关型配置。默认值 true 代表作者推荐的初始行为；若要改动，建议一次只改一个开关便于观察影响。
- Shadow Thickness（阴影厚度）
 类型：数值；默认：20.0f
 说明：用于细调显示样式和间距。默认值 20.0f 通常是平衡视觉效果与紧凑度的设置，建议小步调整并实时观察。
- Start Pos X Rate Min（起始点横坐标比例最小值）
 类型：数值；默认：0.4f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 0.4f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Start Pos X Rate Max（起始点横坐标比例最大值）
 类型：数值；默认：0.6f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 0.6f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Start Pos Y Rate Min（起始点纵坐标比例最小值）
 类型：数值；默认：1.0f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 1.0f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Start Pos Y Rate Max（起始点纵坐标比例最大值）
 类型：数值；默认：1.1f
 说明：用于控制界面元素在屏幕中的相对位置。默认值 1.1f 一般是作者调过的稳定布局；建议每次只改一个轴，避免元素跑出可视区域。
- Rain Effect（雨效果）
 类型：枚举；默认："Off"
 说明：这是选项型配置。默认值 Off 一般更稳妥；建议按使用场景逐个试用，而不是一次性切换多项。
 可选：Off（关闭）；Rise（上升）；Fall（下落）
- Rain Effect Coordinate Mode（雨效果坐标模式）
 类型：枚举；默认："Independent"
 说明：这是该模块的核心行为开关，不同选项对应不同执行策略。建议先保持默认 Independent ，确认稳定后再逐项切换比较效果。
 可选：Follow Key Box（跟随按键框）；Independent（独立）
- Rain Effect Color（雨效果颜色）
 类型：枚举；默认："Rainbow"
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
 可选：见 [NAMED_COLOR_BASE_LIST](./NAMED_COLOR_BASE_LIST.md)
- Rain Effect Opacity (0~1)（雨效果不透明度 (0~1)）
 类型：数值；默认：0.5f
 说明：用于控制透明度。默认值 0.5f 兼顾可见性和遮挡；如果你觉得挡视线可小幅下调，若看不清可小幅上调。
- Rain Effect Whiteness (0~1)（雨效果变白程度 (0~1)）
 类型：数值；默认：0.8f
 说明：这是数值型配置。默认值 0.8f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Rain Effect Width Rate (0~1)（雨效果宽度比例 (0~1)）
 类型：数值；默认：0.8f
 说明：这是数值型配置。默认值 0.8f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Rain Effect Height（雨效果高度）
 类型：数值；默认：250
 说明：这是数值型配置。默认值 250 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Rain Effect Fade Height（雨效果淡出高度）
 类型：数值；默认：60
 说明：这是数值型配置。默认值 60 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Rain Effect Speed（雨效果速率）
 类型：数值；默认：10.0f
 说明：这是数值型配置。默认值 10.0f 通常在稳定性与效果之间做了平衡，建议小步调整并观察实际变化。
- Shadow Color（阴影颜色）
 类型：枚举；默认："Black"
 说明：用于控制视觉配色。建议先选对比度高的配色保证可读性；若是动态颜色，注意在复杂背景下的辨识度。
 可选：见 [NAMED_COLOR_BASE_LIST](./NAMED_COLOR_BASE_LIST.md)
历史更新
- 17. 尝试修复 Keystrokes2 对按下按键检测不灵敏的问题。没成功，，
- 3. Keystrokes2 按键显示的方框大小和间隙可以调节了。
- 7. 为 Keystrokes2 添加了丝滑动画。添加配置项：

备注
该模块可能受系统版本、权限级别、目标进程状态或安全软件策略影响；若功能未生效，优先检查管理员权限、驱动依赖、联网状态与系统兼容性。

相关命令
无

相关模块
- [AntiAFK (反挂机)](./AntiAFK.md)
- [AutoClicker (连点器)](./AutoClicker.md)
- [MouseWatcher (鼠标监视)](./MouseWatcher.md)
- [MouseSwap (鼠标交换)](./MouseSwap.md)
- [MouseDisabler (禁用鼠标)](./MouseDisabler.md)
- [MouseTeleport (鼠标传送)](./MouseTeleport.md)
- [MouseTrails (鼠标轨迹)](./MouseTrails.md)
- [MouseESP (鼠标透视)](./MouseESP.md)

相关资料
无

