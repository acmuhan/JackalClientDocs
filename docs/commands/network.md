# 网络命令

> 本页由 `test/COMMANDS.txt` 自动拆分生成。

共 26 组命令。

## 准备阶段

第一步：打开左侧菜单栏的「命令面板」
随后，在搜索框中输入命令前缀快速定位。

## 核心操作

### `/ipaddress [ip1;ip2;...]`

```text
可用别名：
/ipaddress [ip1;ip2;...]
/ipaddressex [ip1;ip2;...]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
显示 IP 地址地理位置信息。如果不填参数则显示当前 IP 地址位置。/ipaddressex 命令显示人工智能对该地的描述。
```

应用场景：

- 当你需要 IP 地址地理位置信息时，使用此命令

### `/iphorror [ip1;ip2;...]`

```text
可用别名：
/iphorror [ip1;ip2;...]
/iphorrorex [ip1;ip2;...]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
显示 IP 地址恐吓段落。如果不填参数则显示当前 IP 地址位置。/ipaddressex 命令则使用人工智能生成段落。
```

应用场景：

- 当你需要 IP 地址恐吓段落时，使用此命令

### `/tcptable`

```text
可用别名：
/tcptable
/tcptable2
/tcptable3
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
获取当前计算机的所有 TCP 连接。/tcptable2 还会获取外部IP的具体位置。（外国IP只能具体到国家）。 /tcptable3 还会显示该IP的大洲、地区、经纬度。
```

应用场景：

- 当你需要当前计算机的所有 TCP 连接时，使用此命令

### `/irc`

```text
可用别名：
/irc
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
切换IRC聊天室模块状态。当启用时，会自动检查用户名以及状态。启用后就可以接受聊天室消息了。
```

应用场景：

- 当你需要切换IRC聊天室模块状态时，使用此命令

### `/irc send [msg...]`

```text
可用别名：
/irc send [msg...]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
发送一条聊天室公共信息。如果你启用了IRC模块的Fast Send With # Prefix Command选项，你可以直接使用#开头的字符串快捷发送消息。例如：#hello-world
如果不填msg，将会打开一个框，你可以在里面输入（也可以输入中文）。或者使用 ## 也可以。
当前不支持私聊。友善交流讨论。
```

应用场景：

- 当你需要发送一条聊天室公共信息时，使用此命令

### `/irc getname`

```text
可用别名：
/irc getname
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
获取自己的实际用户名。
```

应用场景：

- 当你需要自己的实际用户名时，使用此命令

### `/irc register <name>`

```text
可用别名：
/irc register <name>
/irc setname <name>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
注册当前计算机的聊天室用户名称。名称为 name ，不能含空格。如果检测到名称被占用且不为自己，就不能修改。
```

应用场景：

- 当你需要注册当前计算机的聊天室用户名称时，使用此命令

### `/irc queryuser <name>`

```text
可用别名：
/irc queryuser <name>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
查询 name 用户名是否被占用。
```

应用场景：

- 当你需要查询 name 用户名是否被占用时，使用此命令

### `/irc block <name>`

```text
可用别名：
/irc block <name>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
[PRO] 屏蔽指定用户的消息。会添加到模块配置中，随时可以改。收到被屏蔽用户的消息时，什么也不会显示。
```

应用场景：

- 当你需要[PRO] 屏蔽指定用户的消息时，使用此命令

### `/irc unblock <name>`

```text
可用别名：
/irc unblock <name>
/irc unblock all/*
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
[PRO] 取消屏蔽指定用户的消息。
```

应用场景：

- 当你需要[PRO] 取消屏蔽指定用户的消息时，使用此命令

### `/what`

```text
可用别名：
/what
/wtf
/what [text...=clipboard]
/wtf [text...=clipboard]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
让 AI 解读一段文本。
```

应用场景：

- 当你需要让 AI 解读一段文本时，使用此命令

### `/what is <term>`

```text
可用别名：
/what is <term>
/wtf is <term>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
【PRO】在 Wikipedia 上搜索有关词汇的信息。
```

应用场景：

- 当你需要【PRO】在 Wikipedia 上搜索有关词汇的信息时，使用此命令

### `/what window [hwnd=it]`

```text
可用别名：
/what window [hwnd=it]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
让 AI 解读一个窗口（会自动OCR）
```

应用场景：

- 当你需要让 AI 解读一个窗口（会自动OCR）时，使用此命令

### `/what process [pid=it]`

```text
可用别名：
/what process [pid=it]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
让 AI 解读一个进程
```

应用场景：

- 当你需要让 AI 解读一个进程时，使用此命令

### `/ncm <args...>`

```text
可用别名：
/ncm <args...>
/cloudmusic <args...>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
网易云音乐相关命令。
```

应用场景：

- 当你需要网易云音乐相关命令时，使用此命令

### `/ncm song <keywords...>`

```text
可用别名：
/ncm song <keywords...>
/ncm songex <page> <keywords...>
/ncm songraw <keywords...>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
搜索单曲。songraw 输出原始 json 数据。
```

应用场景：

- 当你需要搜索单曲时，使用此命令

### `/ncm songdetails/songinfo <id>`

```text
可用别名：
/ncm songdetails/songinfo <id>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
获取单曲的详细信息。
```

应用场景：

- 当你需要单曲的详细信息时，使用此命令

### `/ncm getid`

```text
可用别名：
/ncm getid
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
查找一首歌曲的ID。需要输入标题和作曲家字符串。
```

应用场景：

- 当你需要查找一首歌曲的ID时，使用此命令

### `/ncm clearcache`

```text
可用别名：
/ncm clearcache
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
清除封面图片缓存。output\Cover\NCM_Cache
```

应用场景：

- 当你需要清除封面图片缓存时，使用此命令

### `/ncm lyrics <songid>`

```text
可用别名：
/ncm lyrics <songid>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
根据歌曲ID获取没有时间轴的完整歌词。将会异步获取。
```

应用场景：

- 当你需要根据歌曲ID获取没有时间轴的完整歌词时，使用此命令

### `/ncm elog`

```text
可用别名：
/ncm elog
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
【实验性】解析并输出当前网易云ELOG内容。
```

应用场景：

- 当你需要【实验性】解析并输出当前网易云ELOG内容时，使用此命令

### `/ncm checkdll`

```text
可用别名：
/ncm checkdll
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
【PRO】检查 version.dll 网易云监听DLL是否被安装，如果没有被安装则安装并重新启动网易云音乐。
```

应用场景：

- 当你需要【PRO】检查 version.dll 网易云监听DLL是否被安装，如果没有被安装则安装并重新启动网易云音乐时，使用此命令

### `/download <url...>`

```text
可用别名：
/download <url...>
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
开始一个下载任务。url必须以http开头。
```

应用场景：

- 当你需要开始一个下载任务时，使用此命令

### `/download list`

```text
可用别名：
/download list
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
打开 Download HUD 模块。
```

应用场景：

- 当你需要打开 Download HUD 模块时，使用此命令

### `/search [text...]`

```text
可用别名：
/search [text...]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
搜索相关内容，可以在命令行中交互。
```

应用场景：

- 当你需要搜索相关内容，可以在命令行中交互时，使用此命令

### `/searchai [text...]`

```text
可用别名：
/searchai [text...]
```

操作步骤：

1. 在命令面板输入上述任意别名
2. 按下回车键执行命令

功能说明：

```text
搜索相关内容，可以在命令行中交互。且每一个条目都会用AI简短地总结。
```

应用场景：

- 当你需要搜索相关内容，可以在命令行中交互时，使用此命令

## 运行与测试

1. 执行命令后，观察底部状态栏的反馈信息
2. 若命令未生效，检查输入格式是否正确

## 进阶建议

尝试组合多个命令，实现更复杂的操作流程。

恭喜你掌握了本节所有命令，现在去构建你的专属工作流吧！
